<p id="notice"><%= notice %></p>

<p>
  <strong>User:</strong>
  <%= @comment.user_id %>
</p>

<p>
  <strong>Content:</strong>
  <%= @comment.content %>
</p>

<%= link_to 'Edit', edit_user_posting_comment_path(@user, @posting, @comment) %> |
<%= link_to 'Back', user_posting_comments_path(@user, @posting) %>
