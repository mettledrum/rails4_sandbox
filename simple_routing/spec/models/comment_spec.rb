# validate :cannot_form_a_loop
# validate :cannot_be_parent_of_self
#  validate :child_cannot_belong_to_different_posting
#  validate :parent_must_exist_if_id_is_given


require 'spec_helper'

describe Division do
#   it 'generates a tree correctly' do
#   end


#   it 'does not allow loops to be created' do
#     expect { divisions(:d1).update_attributes!(parent_id: divisions(:d1).id)   }.to raise_error(ActiveRecord::RecordInvalid)
#   end

#   it 'does not allow cycles to be created' do
#     expect { divisions(:d1).update_attributes!(parent_id: divisions(:d111).id) }.to raise_error(ActiveRecord::RecordInvalid)
#   end

end
