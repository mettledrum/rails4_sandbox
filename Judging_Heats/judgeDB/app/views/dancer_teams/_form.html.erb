<%= form_for(@dancer_team) do |f| %>
  <% if @dancer_team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dancer_team.errors.count, "error") %> prohibited this dancer_team from being saved:</h2>

      <ul>
      <% @dancer_team.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :team_id %><br>
    <%= f.number_field :team_id %>
  </div>
  <div class="field">
    <%= f.label :dancer_id %><br>
    <%= f.number_field :dancer_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
